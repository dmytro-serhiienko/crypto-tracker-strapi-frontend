import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'crypto-tracker-strapi-frontend',
  webDir: 'dist'
};

export default config;
